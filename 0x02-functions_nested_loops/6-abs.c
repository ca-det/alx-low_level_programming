#include "main.h"
#include <stdio.h>

/**
 * _abs-absolution
 * Return: a few argumentts fir each case
 * @n:used parameter
*/

int _abs(int n)
{
int m;

if (n < 0)
{
m = n * -1;
return (m);
}
else  if (n == 0)
{
return (n);
}
else
{
return (n);
}
}
