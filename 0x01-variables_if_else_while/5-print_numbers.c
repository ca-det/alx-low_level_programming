#include <stdio.h>
/**
 * main-prining my numbers
 *
 *Return: 0 (Success)
*/

int main(void)
{
int decimal;

for (decimal = 0 ; decimal < 10 ; decimal++)
{
printf("%d", decimal);
}
putchar('\n');
return (0);
}

